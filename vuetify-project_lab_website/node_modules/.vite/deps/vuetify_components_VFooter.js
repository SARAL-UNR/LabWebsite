import { Kn as ref, Qn as toRef, U as computed, Yn as shallowRef, _n as watchEffect, ar as normalizeClass, et as createVNode, sr as normalizeStyle } from "./vue.runtime.esm-bundler-CJPQuahF.js";
import { l as propsFactory, n as genericComponent, p as convertToUnit } from "./defineComponent-Diu2h9X6.js";
import { n as makeComponentProps, t as useRender } from "./useRender-BBR-0soC.js";
import { n as useBorder, t as makeBorderProps } from "./border-DemkQlC8.js";
import { n as useElevation, t as makeElevationProps } from "./elevation-eN_Gkreh.js";
import { n as useRounded, t as makeRoundedProps } from "./rounded-C1NJtUsM.js";
import { t as makeTagProps } from "./tag-CDrvHay7.js";
import { i as provideTheme, r as makeThemeProps } from "./theme-2oblNnc1.js";
import { t as useBackgroundColor } from "./color-rbJSGmMJ.js";
import { t as useToggleScope } from "./toggleScope-NnM3a-Nu.js";
import { t as useResizeObserver } from "./resizeObserver-zZpAKajn.js";
import { a as useLayoutItem, n as makeLayoutItemProps } from "./layout-CNSQYTTI.js";
import "/home/cookie/Code/LabWebsite/vuetify-project_lab_website/node_modules/vuetify/lib/components/VFooter/VFooter.css";
//#region node_modules/vuetify/lib/components/VFooter/VFooter.js
var makeVFooterProps = propsFactory({
	app: Boolean,
	color: String,
	height: {
		type: [Number, String],
		default: "auto"
	},
	...makeBorderProps(),
	...makeComponentProps(),
	...makeElevationProps(),
	...makeLayoutItemProps(),
	...makeRoundedProps(),
	...makeTagProps({ tag: "footer" }),
	...makeThemeProps()
}, "VFooter");
var VFooter = genericComponent()({
	name: "VFooter",
	props: makeVFooterProps(),
	setup(props, { slots }) {
		const layoutItemStyles = ref();
		const { themeClasses } = provideTheme(props);
		const { backgroundColorClasses, backgroundColorStyles } = useBackgroundColor(() => props.color);
		const { borderClasses } = useBorder(props);
		const { elevationClasses } = useElevation(props);
		const { roundedClasses } = useRounded(props);
		const autoHeight = shallowRef(32);
		const { resizeRef } = useResizeObserver((entries) => {
			if (!entries.length) return;
			autoHeight.value = entries[0].target.clientHeight;
		});
		const height = computed(() => props.height === "auto" ? autoHeight.value : parseInt(props.height, 10));
		useToggleScope(() => props.app, () => {
			const layout = useLayoutItem({
				id: props.name,
				order: computed(() => parseInt(props.order, 10)),
				position: toRef(() => "bottom"),
				layoutSize: height,
				elementSize: computed(() => props.height === "auto" ? void 0 : height.value),
				active: toRef(() => props.app),
				absolute: toRef(() => props.absolute)
			});
			watchEffect(() => {
				layoutItemStyles.value = layout.layoutItemStyles.value;
			});
		});
		useRender(() => createVNode(props.tag, {
			"ref": resizeRef,
			"class": normalizeClass([
				"v-footer",
				themeClasses.value,
				backgroundColorClasses.value,
				borderClasses.value,
				elevationClasses.value,
				roundedClasses.value,
				props.class
			]),
			"style": normalizeStyle([
				backgroundColorStyles.value,
				props.app ? layoutItemStyles.value : { height: convertToUnit(props.height) },
				props.style
			])
		}, slots));
		return {};
	}
});
//#endregion
export { VFooter };

//# sourceMappingURL=vuetify_components_VFooter.js.map